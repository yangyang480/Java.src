package variableconstantoperator;

public class VariableJava {

    public static void main(String[] args) {
        double radius=20.0; //this is datatype indentifier (can start with letter underscore )

        double area;

        area = radius * radius * 3.14;
        System.out.println("the area is" + " " +area); //sout is the shortcut
    }
}
